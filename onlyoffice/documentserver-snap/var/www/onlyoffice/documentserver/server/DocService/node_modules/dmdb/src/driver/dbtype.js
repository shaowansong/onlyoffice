"use strict";

Object.defineProperty(exports, "__esModule", {
    value: !0
});

var const_1 = require("../desc/const"), DBType = function() {
    function e() {}
    return e.isComplexType = function(t, n) {
        return t === this.BLOB && n === this.COMPLEX_SCALE || t === this.ARRAY || t === this.SARRAY || t === this.CLASS || t === this.PLTYPE_RECORD;
    }, e.isLocalTimeZone = function(t, n) {
        return t === e.DATETIME && 0 != (n & e.LOCAL_TIME_ZONE_SCALE_MASK);
    }, e.getLocalTimeZoneScale = function(t, n) {
        return n & ~e.LOCAL_TIME_ZONE_SCALE_MASK;
    }, e.emptyStringToNull = function(t) {
        switch (t) {
          case e.BOOLEAN:
          case e.BIT:
          case e.TINYINT:
          case e.SMALLINT:
          case e.INT:
          case e.BIGINT:
          case e.REAL:
          case e.DOUBLE:
          case e.DECIMAL:
          case e.DATE:
          case e.TIME:
          case e.DATETIME:
          case e.INTERVAL_DT:
          case e.INTERVAL_YM:
          case e.TIME_TZ:
          case e.DATETIME_TZ:
            return !0;

          default:
            return !1;
        }
    }, e.isFloat = function(t, n) {
        return t === e.DECIMAL && n === e.FLOAT_SCALE_MASK;
    }, e.getFloatPrec = function(t) {
        return Math.round(.30103 * t) + 1;
    }, e.getFloatScale = function(t) {
        return t & ~e.FLOAT_SCALE_MASK;
    }, e.isBFile = function(t, n, c) {
        return t === e.VARCHAR && n === e.BFILE_PREC && c === e.BFILE_SCALE;
    }, e.checkBFileStr = function(t) {
        var n = t.trim().split(":");
        return 2 === n.length && !(128 < n[0].length || 256 < n[1].length);
    }, e.CHAR = 0, e.VARCHAR2 = 1, e.VARCHAR = 2, e.BIT = 3, e.TINYINT = 5, e.SMALLINT = 6, 
    e.INT = 7, e.BIGINT = 8, e.DECIMAL = 9, e.REAL = 10, e.DOUBLE = 11, e.BLOB = 12, 
    e.BOOLEAN = 13, e.DATE = 14, e.TIME = 15, e.DATETIME = 16, e.BINARY = 17, e.VARBINARY = 18, 
    e.CLOB = 19, e.INTERVAL_YM = 20, e.INTERVAL_DT = 21, e.TIME_TZ = 22, e.DATETIME_TZ = 23, 
    e.NULL = 25, e.ANY = 31, e.STAR_ALL = 32, e.STAR = 33, e.RECORD = 40, e.TYPE = 41, 
    e.TYPE_REF = 42, e.UNKNOWN = 54, e.ARRAY = 117, e.CLASS = 119, e.CURSOR = 120, e.PLTYPE_RECORD = 121, 
    e.SARRAY = 122, e.CURSOR_ORACLE = -10, e.BIT_PREC = const_1.Const.BYTE_SIZE, e.TINYINT_PREC = const_1.Const.BYTE_SIZE, 
    e.SMALLINT_PREC = const_1.Const.USINT_SIZE, e.INT_PREC = const_1.Const.ULINT_SIZE, 
    e.BIGINT_PREC = const_1.Const.LINT64_SIZE, e.REAL_PREC = 4, e.DOUBLE_PREC = 8, e.DATE_PREC = 3, 
    e.TIME_PREC = 5, e.DATETIME_PREC = 8, e.TIME_TZ_PREC = 7, e.DATETIME_TZ_PREC = 10, 
    e.VARCHAR_PREC = 8188, e.VARBINARY_PREC = 8188, e.BLOB_PREC = const_1.Const.INT32_MAX, 
    e.CLOB_PREC = const_1.Const.INT32_MAX, e.NULL_PREC = 0, e.LOCAL_TIME_ZONE_SCALE_MASK = 4096, 
    e.BFILE_PREC = 512, e.BFILE_SCALE = 6, e.COMPLEX_SCALE = 5, e.CURRENCY_PREC = 19, 
    e.CURRENCY_SCALE = 4, e.FLOAT_SCALE_MASK = 129, e;
}();

exports.DBType = DBType;